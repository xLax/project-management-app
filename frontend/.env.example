# Root domain of the backend — Vite bakes this into the production bundle.
# Do NOT include /api at the end.
# Set this in Vercel's Environment Variables dashboard before deploying.
VITE_API_URL=https://your-api.herokuapp.com

# Backend URL for the Vite dev-server proxy (dev only)
VITE_API_TARGET=http://localhost:5000
